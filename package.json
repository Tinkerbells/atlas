{
  "name": "atlas-monorepo",
  "type": "module",
  "version": "1.0.0",
  "description": "Atlas - Cross-platform file manager inspired by VSCode",
  "homepage": "https://github.com/atlas/atlas",
  "author": {
    "name": "Atlas Team"
  },
  "private": true,
  "main": "packages/electron-main/entry-point.mjs",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "version": "conventional-changelog -i CHANGELOG.md -s -r 0 && git add CHANGELOG.md",
    "lint": "npm -ws run --if-present lint",
    "build": "npm run build -ws --if-present",
    "compile": "npm run build && electron-builder build --config electron-builder.mjs",
    "compile:mac": "npm run build && electron-builder build --config electron-builder.mjs --mac",
    "compile:win": "npm run build && electron-builder build --config electron-builder.mjs --win",
    "compile:linux": "npm run build && electron-builder build --config electron-builder.mjs --linux",
    "electron:dev": "node scripts/ensure-electron.mjs && npm run dev -w @atlas/electron-main"
  },
  "dependencies": {
    "@atlas/electron-main": "*"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "@npmcli/map-workspaces": "5.0.3",
    "conventional-changelog": "7.1.1",
    "electron": "41.0.3",
    "electron-builder": "26.8.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "engines": {
    "node": ">= 22.12.0 || >= 24.0.0",
    "typescript": ">= 5.9.0 < 5.10.0"
  }
}
