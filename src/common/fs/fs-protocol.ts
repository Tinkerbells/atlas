import type { IQueries, IEvents } from "~/common/bridge";

export interface IFsQueries extends IQueries {
  // пока пусто — добавлять по мере необходимости
}

export interface IFsEvents extends IEvents {
  // пока пусто — добавлять по мере необходимости
}
